// Frontend configuration file
// This password is used for all users and should not be exposed to clients
export const APP_CONFIG = {
  // This password should be kept secret and not exposed to users
  // In a production environment, this should be stored securely (e.g., environment variables)
  LOGIN_PASSWORD: "securePassword123",
  
  // API endpoints
  API_BASE_URL: 'http://localhost:3001',
  
  // Other frontend configuration options can be added here
};
